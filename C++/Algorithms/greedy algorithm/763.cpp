#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

class Solution
{
public:
    vector<int> partitionLabels(string S)
    {
        vector<int> a;
        return a;
    }
};

int main()
{
    string S;
    cin >> S;
    Solution a;
    a.partitionLabels(S);

    system("pause");
    return 0;
}